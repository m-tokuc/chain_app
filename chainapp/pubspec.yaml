name: chainapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.5.1
  provider: ^6.1.1 # Durum yönetimi için
  # Firebase Core & Plugins
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  firebase_messaging: ^16.1.0
  
  # Google Sign In (Burada da sabitliyoruz)
  google_sign_in: 6.2.1
  
  # Yardımcı Paketler
  uuid: ^4.2.2
  intl: ^0.20.2
  share_plus: ^12.0.1
  timezone: ^0.9.2
  cupertino_icons: ^1.0.6

  # Bildirimler
  flutter_local_notifications: ^17.2.2 
  flutter_timezone: ^4.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/hsl.login/hsl.jpg
    - assets/images/panda/
    - assets/alarm.mp3  # Baş harfinin büyük olduğuna dikkat et!
# 🔥 DÜZELTME: TÜM OVERRIDE'LAR TEK BİR YERDE TOPLANDI
dependency_overrides:
  flutter_local_notifications: ^17.2.2
  google_sign_in: 6.2.1
  google_sign_in_android: 6.1.31